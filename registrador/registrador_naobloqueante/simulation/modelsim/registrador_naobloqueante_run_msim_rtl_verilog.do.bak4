transcript on
if {[file exists rtl_work]} {
	vdel -lib rtl_work -all
}
vlib rtl_work
vmap work rtl_work

vlog -vlog01compat -work work +incdir+C:/altera/registrador_naobloqueante {C:/altera/registrador_naobloqueante/registrador_naobloqueante.v}

